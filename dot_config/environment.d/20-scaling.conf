# Qt5 + Qt6: 1.35x UI zoom (AUTO=0 so Qt doesn't stack auto-scale on top)
QT_SCALE_FACTOR=1.35
QT_AUTO_SCREEN_SCALE_FACTOR=0
# GTK3/GTK4: integer UI scale stays 1, fonts x1.35 (GDK can't do fractional UI)
GDK_SCALE=1
GDK_DPI_SCALE=1.35
