# ==============================================================================
# WirePlumber Bluetooth Policy & Latency Optimization
# ==============================================================================
# - Enables seamless auto-switching to HFP (mSBC Wideband Speech) when mic is needed
# - Prioritizes AAC & SBC-XQ with dedicated 1024/48000 buffer for glitch-free audio
# ==============================================================================

wireplumber.settings = {
  bluetooth.autoswitch-to-headset-profile = true
}

monitor.bluez.properties = {
  bluez5.enable-sbc-xq = true
  bluez5.enable-msbc = true
  bluez5.codecs = [ aac sbc_xq sbc ]
}

monitor.bluez.rules = [
  {
    matches = [
      {
        node.name = "~bluez_output.*"
      }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
        node.latency = "1024/48000"
      }
    }
  },
  {
    matches = [
      {
        node.name = "~bluez_input.*"
      }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
        node.latency = "1024/48000"
      }
    }
  }
]
